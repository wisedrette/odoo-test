deployments:
  odoo:
    pre_launch:
      - target: db
        command: port
        options: "{target}"
    post_launch:
      - target: db
        command: exec
        options: "{target} bash -c 'echo test && echo test > test'"
      - target: web
        command: exec
        options: "{target} bash -c 'ls -la ./'"
